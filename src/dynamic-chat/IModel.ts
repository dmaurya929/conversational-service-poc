// subtype of DC chat, needs to evaluates if this is required for forms
export enum CardSubType {
    EMAIL="email",
    PHONE="phone",
    TEXT="text",
    BOOLEAN="boolean",
    INTEGER="integer",
    FLOAT="float",
    DATE="date",
    DATETIME="datetime"
}
